// Estructura que representa un aporte con fecha y monto
public struct Aporte
{
    public string Fecha;
    public float Monto;

    public Aporte(string fecha, float monto)
    {
        Fecha = fecha;
        Monto = monto;
    }
}
